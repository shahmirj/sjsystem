[user]
    name = Shahmir Javaid
    email = shahmirj@gmail.com
[credential]
    helper = cache --timeout=3600
    username = shahmirj@gmail.com
[color]
    ui = true
    pager = true
    status = true
[merge]
    commit = no
    ff = false
[alias]
    br = branch
    br = branch
    brf = branch -f
    bi = bisect
    ci = commit
    cia = commit --amend
    co = checkout
    cob = checkout -b
    cp = cherry-pick
    di = diff --color --ws-error-highlight=new,old
    fe = fetch
    me = merge
    re = rebase
    rea = rebase --abort
    rec = rebase --continue
    rei = rebase --interactive
    search = grep -n
    st = status --short -b
    up = pull --rebase
    
    gr = log --graph --oneline --topo-order --all --decorate --boundary
    gro = log --remotes=origin --graph --oneline --topo-order --decorate --boundary
    tree = log --graph --oneline --decorate
[push]
    default = current
[pull]
    default = current
    rebase = true
[fetch]
    prune = true
[filter "media"]
    required = true
    clean = git media clean %f
    smudge = git media smudge %f
[filter "hawser"]
    clean = git hawser clean %f
    smudge = git hawser smudge %f
    required = true
[filter "lfs"]
    clean = git-lfs clean %f
    smudge = git-lfs smudge %f
    required = true
[diff]
    colorMoved=default
